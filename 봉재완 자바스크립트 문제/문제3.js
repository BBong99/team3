// 문제 3: 짝수만 필터링후 제곱으로 반환

function filterEvenNumbers(array) {
  // 여기에 함수를 작성하세요.
};


const numbers = [1, 2, 3, 4, 5, 6];
const evens = filterEvenNumbers(numbers);
console.log(evens); // [4, 16, 36]